export const API_ROOT = (process.env.NODE_ENV === 'production')
  ? 'http://hh.wangziqing.cc/'
  : 'http://hh.wangziqing.cc/'

export const CookieDomain = (process.env.NODE_ENV === 'production')
  ? 'localhost'
  : ''
